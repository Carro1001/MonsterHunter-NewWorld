pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

plugins {
    // Lets Gradle auto-provision the Java 21 toolchain the 1.21.1 target requires.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'MonsterHunter-NewWorld'
